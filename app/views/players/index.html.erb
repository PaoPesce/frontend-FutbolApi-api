<h1>Jugadores de Futbol</h1>
<% @players.each do |player| %>
  <p><%= link_to player["name"], player_path(player["id"]) %></p>
  <p><%= player["team"] %></p>
  <p><%= player["position"] %></p>
  <p><%= player["number"] %></p>

  <img src="<%= player["images_url"] %>" alt="">
<% end %>
